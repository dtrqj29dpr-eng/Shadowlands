// Preload script — runs in the renderer before page scripts, with Node.js access.
// contextIsolation is enabled and nodeIntegration is disabled in the main process.
// Expose any Electron APIs to the renderer here via contextBridge if ever needed:
//
//   import { contextBridge } from 'electron';
//   contextBridge.exposeInMainWorld('electronAPI', { ... });
