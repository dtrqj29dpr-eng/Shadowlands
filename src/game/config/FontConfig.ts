/**
 * Central font config. To change the game font, update this one value.
 * GAME_FONT_FAMILY is used by every Phaser text style in HUD, inventory,
 * tooltips, loot feed, etc.
 *
 * When a bundled custom font is ready:
 *  1. Add the .woff2 to public/assets/fonts/
 *  2. Add an @font-face in index.html
 *  3. Change the value below to '"MyFont", sans-serif'
 */
export const GAME_FONT_FAMILY = '"Segoe UI", Arial, sans-serif';
